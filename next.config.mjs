/** @type {import('next').NextConfig} */
const nextConfig = {
	images: {
		domains: ["images.pexels.com", "pngtree.com", "res.cloudinary.com"], // Add multiple hostnames here
	},
};

export default nextConfig;
